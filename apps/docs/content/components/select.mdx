import { SelectDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# Select

Dropdown select built on Radix Select primitives.

<StorybookLink package="ui" story="core-select" />

## Example

<ComponentPreview>
  <SelectDemo />
</ComponentPreview>

## Components

`Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`, `SelectLabel`, `SelectSeparator`, `SelectGroup`. `SelectTrigger` accepts `inputSize`.

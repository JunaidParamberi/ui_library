import { CardDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# Card

Surface container with header, body, and footer slots.

<StorybookLink package="ui" story="core-card" />

## Example

<ComponentPreview>
  <CardDemo />
</ComponentPreview>

## Components

`Card`, `CardHeader`, `CardTitle`, `CardBody`, `CardFooter` — compose a card layout. `Card` accepts `interactive` and `dashed` variants.

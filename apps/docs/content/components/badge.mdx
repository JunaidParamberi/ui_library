import { BadgeDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# Badge

Status and label chip with semantic color variants.

<StorybookLink package="ui" story="core-badge" />

## Example

<ComponentPreview>
  <BadgeDemo />
</ComponentPreview>

## Props

| Prop | Type | Default |
|------|------|---------|
| `variant` | `"success" \| "warning" \| "danger" \| "info" \| "accent" \| "outline"` | `"accent"` |
| `dot` | `boolean` | `false` |

# Consume in an app

1. Import `@manpowerhub/tokens/globals.css` once in your app stylesheet.
2. Extend the tokens Tailwind preset in `tailwind.config.ts`.
3. Import components from `@manpowerhub/ui` or `@manpowerhub/blocks`.

See `apps/playground` for a working Vite reference.

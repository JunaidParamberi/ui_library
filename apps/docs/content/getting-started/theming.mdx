# Theming

Override CSS variables in `:root` (light) and `.dark` (dark) to reskin globally.
Semantic Tailwind classes (`bg-primary`, `text-foreground`, etc.) map to those vars via `@manpowerhub/tokens/preset`.

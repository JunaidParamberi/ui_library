import { DataTableToolbarDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# DataTableToolbar

Search, filter selects, and action slot for data tables.

<StorybookLink package="blocks" story="blocks-datatabletoolbar" />

## Example

<ComponentPreview>
  <DataTableToolbarDemo />
</ComponentPreview>

## Props

`search` (controlled), `filters` (array of filter configs), `actions` (React node).

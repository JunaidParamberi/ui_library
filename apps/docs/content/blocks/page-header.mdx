import { PageHeaderDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# PageHeader

Page title row with description, badge, breadcrumbs, and actions.

<StorybookLink package="blocks" story="blocks-pageheader" />

## Example

<ComponentPreview>
  <PageHeaderDemo />
</ComponentPreview>

## Props

`title`, optional `description`, `badge`, `actions`, `breadcrumbs`, `bordered`.

import { EmptyStateDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# EmptyState

Centered empty-list placeholder with icon, copy, and optional action.

<StorybookLink package="blocks" story="blocks-emptystate" />

## Example

<ComponentPreview>
  <EmptyStateDemo />
</ComponentPreview>

## Props

`icon`, `title`, `description`, optional `action` (`{ label, onClick }`), `variant`.

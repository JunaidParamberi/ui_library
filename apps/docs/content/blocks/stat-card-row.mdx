import { StatCardRowDemo } from "../../src/components/dynamic-demos";
import { StorybookLink } from "../../src/components/storybook-link";
import { ComponentPreview } from "../../src/components/component-preview";

# StatCardRow

Responsive row of KPI cards for dashboard summaries.

<StorybookLink package="blocks" story="blocks-statcardrow" />

## Example

<ComponentPreview>
  <StatCardRowDemo />
</ComponentPreview>

## Props

`stats` (`KPI[]` from `@manpowerhub/ui`), `columns` (1–4).

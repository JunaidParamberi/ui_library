import { TypeRamp } from "../../src/components/token-swatches";

# Typography

Font stacks: `--font-display`, `--font-body`, `--font-mono`. Use `font-display` / `font-body` Tailwind utilities.

<TypeRamp />
